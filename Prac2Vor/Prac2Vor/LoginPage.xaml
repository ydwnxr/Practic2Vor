<Page x:Class="prac2ivanovamelikyan2.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:prac2ivanovamelikyan2"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoginPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="Aqua">
            <TextBlock Text="Вход в систему" FontSize="25" VerticalAlignment="Center" HorizontalAlignment="Center"></TextBlock>
        </Grid>
        <StackPanel Grid.Row="1" HorizontalAlignment="Center"
            VerticalAlignment="Center" Width="200">
            <TextBlock Text="Логин"></TextBlock>
            <TextBox x:Name="LoginInput"></TextBox>
            <TextBlock Text="Пароль" FontSize="20"></TextBlock>
            <PasswordBox x:Name="PasswordInput"></PasswordBox>
            <Button x:Name="LoginButton" Content="Войти" Margin="0,10,0,0" 
                    Click="LoginButton_Click"></Button>
        </StackPanel>
    </Grid>
</Page>
